---
import { experience } from '~/data/site';
---

<section id="experiencia" class="section bg-slate-50/60">
  <div class="container grid lg:grid-cols-[1fr,2fr] gap-12">
    <div>
      <p class="eyebrow">Experiencia</p>
      <h2 class="heading mt-4">Trayectoria</h2>
      <p class="mt-4 text-slate-500 text-sm">
        Formación académica y experiencia profesional que sostienen mi forma de trabajar.
      </p>
    </div>

    <ol class="relative border-l-2 border-slate-200 space-y-10 pl-6">
      {experience.map((item) => (
        <li class="relative">
          <span class="absolute -left-[33px] top-1 grid h-4 w-4 place-items-center rounded-full border-2 border-white bg-navy shadow">
            <span class="h-1.5 w-1.5 rounded-full bg-white"></span>
          </span>
          <p class="text-xs uppercase tracking-widest text-slate-400 font-semibold">{item.period}</p>
          <h3 class="mt-1 font-display text-xl text-navy">{item.role}</h3>
          <p class="text-slate-500 text-sm">{item.org}</p>
          <p class="mt-3 text-slate-700 leading-relaxed">{item.description}</p>
        </li>
      ))}
    </ol>
  </div>
</section>
